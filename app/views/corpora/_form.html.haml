.container-fluid
  .col-md-12
    = simple_form_for(@corpus) do |f|
      = f.error_notification
      = f.input :name
      = f.input :description
      = f.input :privacy_type
      .actions
        = f.button :button, type: :submit, class: 'btn-submit'